---
description: 塔科夫图鉴上游：只走 json.tarkov.dev，不打 GraphQL
globs: backend/app/services/tarkov/**/*.py,backend/app/api/guides/tarkov*.py,backend/tests/test_tarkov*.py,frontend/src/lib/tarkov*.ts,frontend/src/components/guides/tarkov/**/*,frontend/src/pages/guides/Tarkov*.tsx
alwaysApply: false
---

# 塔科夫图鉴上游

图鉴回源**只打** [json.tarkov.dev](https://json.tarkov.dev)：PVP=`/regular/`，PVE=`/pve/`。编排在 `tarkov.upstream` + `tarkov.sync`；`tarkov_*_raws` 一张上游文件一张表。

## 必须

- 新栏目、新标点、新同步从 **maps/items/tasks 等 dump** 投影，locale 走同资源 `_zh`
- extras（技能 / 跳蚤 / 成就 / 容器目录等）从已下载 dump **抽出**，不另打 GraphQL
- 任务 / 物品 / 制作在 **parse 前**与 `tarkov_overlay_raws`（[tarkov-data-overlay](https://github.com/tarkovtracker-org/tarkov-data-overlay)）内存合入，不改 json 各表、不落合并表
- 互动地图底图仍用 `frontend/src/data/tarkov-dev-maps.json` + `assets.tarkov.dev`（SVG/瓦片）；容器格仓轮廓用 `frontend/src/data/tarkov-item-grids.json`。二者都是展示资源，不是图鉴 API
- 工作台改装中间预览：底板用 dump 枪图（工厂预设 / 裸机匣静图），按槽位 `nameId` 在枪图上点选配件（含空槽）。dump 没有整枪照片坐标。默认**不**代理 [image-gen.tarkov-changes.com](https://image-gen.tarkov-changes.com)。旧出图接口仍在，须显式 `TARKOV_WORKBENCH_IMAGE_GEN=true` 才启用。
- 工作台社区方案：只 **GET** [eftforge.com](https://eftforge.com) `/builds/public?gun_id=`（公开列表），投影成 dump 可装的 `pairs`。不当图鉴源、不代投票/评论、不热链 Gitee 卡图、不落库（短时 KV 除外）。`TARKOV_WORKBENCH_COMMUNITY=false` 可关。
- 工作台枪匠：从 tasks dump 的 `buildWeapon` 投影约束。json dump 用 `buildAttributes` 对象（人机 / 后坐和 / 重量 / 弹匣 / 有效距离 / 耐久 / 格仓），不是 GraphQL 的 `attributes` 数组；`≥0` 与格仓下限是占位。必装件 / 类别组仍走 `containsAll` · `containsCategory`。**不** vendor EFTForge 手抄 JSON。求解是本站约束满足 + 局部替换，不是对方 HiGHS。pytest 不打真站。

## 禁止

- 回源 `api.tarkov.dev` GraphQL（含 maps markers overlay、ammo/guns split）
- 为「补全 GraphQL 才有的字段」再加一条 GraphQL 请求；dump 缺的字段就当 dump 没有
- 把 BTR/门锁等标点改去现场打 GraphQL；走 `maps` raw 投影
- pytest 打真实 json.tarkov.dev / overlay CDN（用夹具）
- pytest 打真实 image-gen.tarkov-changes.com（出图代理用注入假后端）
- pytest 打真实 eftforge.com（社区方案用夹具）
- vendor EFTForge / tarkov-weapon-optimizer 的枪匠 JSON 当本站任务源；枪匠约束从 dump `buildWeapon` 投影

## 兼容

弹药/枪械 parse 仍能读历史 GraphQL split 信封，只为旧 `tarkov_items_raws` 行；**新写入只有 json dump**。不要再扩展 GraphQL 回源。
