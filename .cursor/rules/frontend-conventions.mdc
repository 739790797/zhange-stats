---
description: 前端目录、路由权限、Query/zustand、OpenAPI 类型约定
globs: frontend/**/*.{ts,tsx}
alwaysApply: false
---

# 前端约定

## 目录

- `pages/`：路由页；`components/`：可复用 UI。根上只留跨平台外壳（`CheckinPageTemplate` / `BoxPanelChrome` / `AttendanceCalendarButton` 等）；平台面板进 `components/<platform>/`，攻略进 `guides/`
- `src/data/`：源码资源（如塔科夫地图 JSON）；运行时/缓存在仓库根 `var/`，勿往 `frontend/data` 写
- `api/`：`http.ts` + 域 `*Api.ts`；`client.ts` 再导出
- `stores/authStore.ts`：仅 JWT + 用户（localStorage）
- `lib/apiError.ts`：用户可见错误文案

## 权限与路由

- `PrivateRoute` 需登录；`AdminRoute` 看 `role === admin`（`is_admin` 仅派生展示）
- `PlatformRoute` 受 `platform_features` 控制

## 数据与 UI

- 远端状态用 TanStack Query；勿把服务端列表塞进 zustand
- 路由页一律 `React.lazy`，`AppLayout` 的 Outlet 外包 `Suspense`；禁止把页面静态引进 `App.tsx`
- 平台 `PlatformFeatureTabsPage` 保持 `destroyInactiveTabPane`；盒子 / 兑换 / 养成 Tab 按需 lazy
- UI：Ant Design 5 + `antdAppTheme`；按钮角色/尺寸见 `frontend-ui.mdc`
- **新签到内容**套 `CheckinPageTemplate`（`contentOnly` 嵌入平台 Tabs）
- **新兑换内容**套 `ExchangePageTemplate` + `ExchangeGoodsCard`（商品字段/确认流由平台注入）
- **平台页外壳**套 `PlatformFeatureTabsPage`（绑定门禁 + feature Tabs）
- **新签到日历**套 `AttendanceCalendarButton`（禁止再复制五份 Modal）
- **新盒子**先套 `BoxPanelChrome`（禁止复制未绑定 / 加载 / 失败 / 刷新条）；角色卡仍由面板自己画
- **新扫码绑定**走 `useQrBindSession`（短信/密码/极验仍留 BindPanel；表单走 `PhoneAuthBindTemplate`）
- **认证未登录页**套 `AuthGuestShell`（登录/注册/找回/验证/安装）
- 塔科夫 antd 暗色只走 `TarkovGuideShell` 的 `TarkovThemed` 一层，详情页不要再包 `ConfigProvider`
- 盒子读库优先；**签到展示**打开页始终 `force` 回源（见 `platform-raw-cache`）
- 平台积分/代币兑换：页签与任务配置统一称 **「兑换」**（勿写「库洛币兑换」「积分兑换」等）；面板标题用「兑换商城」，货币名仅在余额/消耗文案中出现

### `force` 传参（签到 status）

四平台 `GET …/status` 后端 **`force` 默认 true**。前端 `fetch*Status`：

- **必须显式把 `force` 放进 query**（`force: true | false`），禁止 `...(force ? { force: true } : {})` 省略——省略时后端仍按默认 true，与 JS 默认 `false` 语义错位。
- `fetch*Status` 的 JS 默认应为 **`force = true`**（对齐契约与展示路径）。
- 调度/读 logs 的调用方须显式传 `force: false`（若走 HTTP；通常调度在 service 层不经此 API）。
- 盒子 / 日历 / rogue 等 raw 接口后端默认 `force=false`；同样建议显式传 `force`，避免歧义。

## 类型

- 优先 `import type { components } from "@/api/generated/schema"` 再 alias
- 新增手写 `interface` 须因 OpenAPI 尚无对应 schema；能派生则不要复制字段

## API 变更

后端契约变更后：`npm run export:openapi && npm run gen:api`，提交 `generated/`。

请求/错误边界见 `frontend-api-errors.mdc`。
