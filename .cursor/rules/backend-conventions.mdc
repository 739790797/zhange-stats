---
description: 后端分层、upstream 拆分、checkin_common、限流与测试
globs: backend/app/**/*.py
alwaysApply: false
---

# 后端约定

## 分层

- `api/`：参数、依赖、HTTP 状态；**不**直接打第三方上游
- `services/`：编排与业务，**按域分包**（`checkin/` · `skland/` · `mihoyo/` · `minecraft/` · `tarkov/` …）；横切可留根上。新平台进 `services/<platform>/`，勿在 `services/` 根新增 `*_client.py`
- `models/`：表结构
- 签到：`checkin.orchestrator` + 各平台 `CheckinPlatformAdapter`；状态/upsert 走 `checkin.common`（今日权威只写 `*_checkin_logs`）
- 新平台签到 checklist：实现 `services/<platform>/client.py` / `attendance.py`（出口 `(session, list[CheckinResult])`）→ 实现 Adapter → 注册进 `checkin.registry.ADAPTERS` → prefs `PLATFORM_*` + Bind/Log/Alembic + API `build_checkin_status` + scheduler 挂 `checkin_job_wrapper`；**勿**再复制 `query_today_for_bind` / `run_checkin_for_bind` 骨架

### Adapter 可选钩子（按需实现，勿抬进 orchestrator）

| 钩子 / 能力 | 何时需要 | 参考 |
|-------------|----------|------|
| `prepare_cached_results` → `None` | logs 命中但展示不完整（如缺 icon）须强制回源 | `SklandCheckinAdapter`（B服豁免） |
| 短时 session / cred 缓存 | 换票贵或上游限频 | `skland.session_cache` + `ephemeral_kv` |
| 鉴权失败 retry 一次 | 缓存票失效常见 | `skland.checkin.query_today_for_bind` 外包层 |
| `STATUS_UNKNOWN` | 上游查询不可信，勿当 `pending`（未签） | 森空岛 B服；见 `skland-upstream.mdc` |

渠道/上游语义分裂留在该平台 `*_attendance`，**不要**为单一平台改公共编排。

## Upstream 客户端

- `*_client.py`：认证、会话、通用 HTTP、常量
- `*_attendance.py`：签到查询/执行
- 可选 `*_boxes.py` / 旁路 raw（日历、肉鸽等）：parse/fetch，模式同盒子
- 为兼容可从 `*_client` re-export；**新逻辑写进拆分模块**，勿继续胀单文件

## 横切

- 时间：`app.core.timeutil`（北京墙钟）
- 密钥：`crypto_secret` Fernet
- 运行时：`DATA_DIR` / `UPLOAD_DIR` 相对路径相对**安装根**（默认 `var/data`、`var/uploads`），见 `app.core.paths`；勿按 cwd 写进 `backend/` / `frontend/`
- 限流 + 短时 KV（扫码 / 森空岛 cred）：`REDIS_URL`；本地可空（进程内降级）。**多 `app` 实例必须共享同一 Redis**，否则扫码 pending / cred 缓存不互通；当前产品仍按单副本设计（见 `docs/deploy.md`「部署形态」）
- 出站 HTTP：`app.core.http_client.http_request`（进程级 httpx）；勿每次新建 `httpx.Client` / `urllib.urlopen`
- QQ 登录：一次性 ticket，勿把 JWT 放回调 query

## 数据库

改 `models/` → Alembic + `docs/database.md`；勿扩 `schema_ensure`（见 `db-schema-readme.mdc`）。

## 测试

分层、必补清单、禁止同层重复：见 `testing.mdc`。  
改平台渠道语义（尤其森空岛官/B服）：**先补 `test_skland_*` / 对应平台测，再改 attendance**。
