# 数据库链接
DATABASE_URL=mysql+pymysql://root:password@127.0.0.1:3306/zhange_stats_dev
# 管理员账号
ADMIN_USERNAME=admin
ADMIN_PASSWORD=123456
ADMIN_DISPLAY_NAME=管理员
ADMIN_EMAIL=admin@localhost
# RESET_ADMIN_PASSWORD=true   # 仅排障时开启：启动时把种子管理员密码重置为 ADMIN_PASSWORD
# ENFORCE_SINGLE_ADMIN=true   # 仅需要时开启：启动时把其它管理员降级为普通用户
# APP_ENV=development  # 本地默认；Docker 镜像为 production（弱口令默认拒绝启动）
# REJECT_WEAK_ADMIN_PASSWORD=true  # 可显式强制；production 下即使不设也会拒绝弱口令
# REJECT_WEAK_ADMIN_PASSWORD=false  # 仅排障：production 下仍允许弱口令启动（不推荐）
# REDIS_URL=redis://127.0.0.1:6379/0  # 可选；多实例部署时限流共用
# ACCESS_TOKEN_EXPIRE_MINUTES=1440  # 默认 24 小时；也可在管理端「安全」调整
# ALLOW_EMAIL_CODE_LOG=true  # 仅本地：SMTP 未配时把验证码打到日志

# OAuth 回调 / CORS 一般无需配置：
# - Docker 同域部署：按浏览器访问的 Host 自动推断
# - 本地 Vite：默认放行 localhost / 127.0.0.1 任意端口
# 特殊反代可选手动覆盖：
# CORS_ORIGINS=https://app.example.com
# PUBLIC_BACKEND_URL=https://app.example.com
# PUBLIC_FRONTEND_URL=https://app.example.com

# Docker部署
APP_PORT=8080
APP_IMAGE=ghcr.io/739790797/zhange-stats
APP_TAG=latest
# Watchtower 轮询间隔（秒），默认 300
WATCHTOWER_POLL_INTERVAL=300
UPDATE_REPO=739790797/zhange-stats
